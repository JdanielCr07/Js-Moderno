//querySelectorall diferencia del queryselector normal, este si te muestra todos los elementos que correspondan 

const card = document.querySelectorAll('.card');//Usa la sintaxis parecida a css en caso de ser clases con .
console.log(card);

const formulario = document.querySelectorAll('#formulario');
console.log(formulario);